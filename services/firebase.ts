export const firebaseConfig = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT_ID.firebaseapp.com',
  projectId: 'YOUR_PROJECT_ID',
  storageBucket: 'YOUR_PROJECT_ID.appspot.com',
  messagingSenderId: 'YOUR_MESSAGING_SENDER_ID',
  appId: 'YOUR_APP_ID',
} as const;

// TODO: Replace the placeholder values above with your real Firebase project config.
// TODO: For @react-native-firebase, add the platform files (google-services.json and GoogleService-Info.plist)
// and wire the native app/auth initialization here when ready.
export const firebaseApp = null;
